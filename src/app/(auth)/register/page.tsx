import RegisterForm from './components/RegisterForm';

const RegisterPage = () => {
	return (
		<main className="w-full h-fit flex py-20 justify-center flex-col items-center">
			<RegisterForm />
		</main>
	);
};

export default RegisterPage;
